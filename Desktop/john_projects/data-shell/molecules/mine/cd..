LOGFILE=script.log

echo "the current user is:" $(users), \"$LOGFILE\" \"." 1>$LOGFILE
echo "The date today  is: "   $(date +%D ) \"$LOGFILE" \"." 1>>$LOGFLE
echo  "The time is: "  $(date +%r) \ "$LOGFILE"\"." 1>>$LOGFILE
echo "My system uptime is:" $(uptime -p) \ "$LOGFILE"\"." 1>>$LOGFILE


